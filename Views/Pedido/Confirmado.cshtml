@{
    ViewData["Title"] = "Pedido confirmado";
    var msg = TempData["Msg"] ?? ViewBag.Msg ?? "Pagamento confirmado com sucesso!";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container mt-5 text-center">
    <div class="card shadow p-4 mx-auto" style="max-width: 560px;">
        <h2 class="mb-3">Pedido confirmado</h2>
        <p class="lead">@msg</p>

        <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Voltar à loja</a>
    </div>
</div>
